<h2>商品詳細</h2>

<%= image_tag @item.get_image(200,100) %>

<h4>商品名</h4>
<%= @item.name %>

<h4>ジャンル</h4>
<%= @item.genre.name %>

<h4>税抜価格<br>（税込価格）</h4>
<%= @item.price%>（<%= @item.tax_in %>）円

<h4>販売ステータス</h4>
<% if @item.is_active == true %>
<p class="text-success">販売中</p>
<% else %>
<p class="text-success">販売停止中</p>
<% end %>

<%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %>
